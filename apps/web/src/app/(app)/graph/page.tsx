import { Workflow } from "lucide-react";
import { EmptyState } from "@/components/shared/empty-state";

export default function GraphPage() {
  return <EmptyState icon={Workflow} title="Nothing here yet" description="Knowledge Graph content is coming in a future session." />;
}
