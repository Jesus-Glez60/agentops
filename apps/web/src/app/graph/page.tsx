export default function GraphPage() {
  return (
    <main className="mx-auto max-w-3xl px-8 py-16">
      <h1 className="text-2xl font-semibold">Neuron / graph browser</h1>
      <p className="mt-2 text-zinc-600 dark:text-zinc-400">
        Phase 1 stub — will render the code graph (symbols, dependencies, gotcha nodes)
        read from agentops-graph via agentops-api. This is the single most demoable
        artifact for the investor pitch.
      </p>
    </main>
  );
}
