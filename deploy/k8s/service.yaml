apiVersion: v1
kind: Service
metadata:
  name: agentops
spec:
  selector:
    app: agentops
  ports:
    - name: web
      port: 3000
      targetPort: web
    - name: api
      port: 8420
      targetPort: api
