[package]
name = "agentops-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "agentops"
path = "src/main.rs"

[dependencies]
anyhow.workspace = true
clap.workspace = true
tokio.workspace = true

agentops-scanner = { path = "../agentops-scanner" }
agentops-graph = { path = "../agentops-graph" }
agentops-notes = { path = "../agentops-notes" }
agentops-llm = { path = "../agentops-llm" }
agentops-security = { path = "../agentops-security" }
agentops-mcp = { path = "../agentops-mcp" }
agentops-api = { path = "../agentops-api" }
docbrain-mcp = { path = "../../../docbrain-core/crates/docbrain-mcp" }
docbrain-api = { path = "../../../docbrain-core/crates/docbrain-api" }

# Re-added when their features are built: agentops-docgen (docgen command),
# agentops-manifest (repo registry/forget command), agentops-ruler-bridge +
# dialoguer/indicatif (prompt-pack distribution, sync-docs' interactive
# fallback) — see the plan's "deliberately out of scope" note.
